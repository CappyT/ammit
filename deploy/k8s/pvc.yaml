apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ammit-report-api-data
  labels:
    app: ammit-report-api
spec:
  accessModes: ["ReadWriteOnce"]
  # storageClassName: ""   # CHANGE ME: set your cluster's StorageClass here.
  #                        # Omit the line entirely to use the cluster default.
  resources:
    requests:
      storage: 1Gi
